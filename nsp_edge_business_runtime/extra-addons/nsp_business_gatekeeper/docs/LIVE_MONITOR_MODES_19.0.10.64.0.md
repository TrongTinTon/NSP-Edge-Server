# Live Monitor modes — 19.0.10.64.0

- Check-in: 8/16/24 columns × 4 rows; plate only; denied Check-in logs never create UI alerts.
- Check-out: two-panel guard layout. Left = 4 × 4 allowed exits with plate + actual detected User. Right = large denied Check-out alerts.
- Check-out identity never falls back to Vehicle Owner when User RFID is missing.
- Snapshot is filtered server-side by selected event type.
