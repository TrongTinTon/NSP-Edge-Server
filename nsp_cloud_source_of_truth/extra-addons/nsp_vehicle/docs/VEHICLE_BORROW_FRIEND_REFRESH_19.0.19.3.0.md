# Vehicle Borrow friend refresh — 19.0.19.3.0

Vehicle Borrow remains Cloud Master Data. The accepted-friend selector now depends on both sent and received Friendship state/participants, so accepting a friend request invalidates the borrower choices correctly. Server-side validation still requires an accepted friendship.
